#ifndef TMC_BLOCKS_SCENE_H
#define TMC_BLOCKS_SCENE_H

namespace TooManyCubes {

/* BlocksScene - holds and maintains blocks (TooManyCubes::Block).
 * Nothing except for a block is expected to be an object
 * in this project so no higher level of abstraction is required. */
class BlocksScene
{
public:
    BlocksScene();

};

} // namespace TooManyCubes

#endif // TMC_BLOCKS_SCENE_H
